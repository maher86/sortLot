name: sortlot-test

services:
  php:
    environment:
      APP_ENV: testing
      APP_DEBUG: "true"
      DB_DATABASE: sortlot_test
      CACHE_STORE: array
      SESSION_DRIVER: array
      QUEUE_CONNECTION: sync
      MAIL_MAILER: array

  mysql:
    environment:
      MYSQL_DATABASE: sortlot_test

  minio:
    profiles:
      - disabled
